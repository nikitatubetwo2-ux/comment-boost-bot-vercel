# 🎯 Следующие улучшения Video Factory

## 🔴 КРИТИЧНО (сильно ускорит/улучшит)

### 1. Whisper для точных субтитров
**Проблема:** Сейчас субтитры генерируются по тексту, не синхронизированы с аудио
**Решение:** Использовать Whisper (бесплатно через HuggingFace) для транскрипции аудио
```python
# Точные таймкоды для каждого слова
from transformers import pipeline
whisper = pipeline("automatic-speech-recognition", model="openai/whisper-base")
result = whisper("audio.mp3", return_timestamps=True)
```
**Результат:** Идеально синхронизированные субтитры

### 2. Кэширование промптов изображений
**Проблема:** Если генерация прервалась — всё начинается заново
**Решение:** Сохранять промпты и результаты в JSON, продолжать с места остановки
```python
# В flux_generator.py
cache_file = output_dir / "generation_cache.json"
if cache_file.exists():
    cache = json.load(cache_file)
    skip_indices = [i for i, r in cache.items() if r["success"]]
```

### 3. Автоматический выбор лучшего токена HF
**Проблема:** Токены исчерпываются, приходится ждать
**Решение:** Проверять лимиты перед генерацией, выбирать токен с наибольшим остатком
```python
def get_best_token(self):
    for token in self.hf_tokens:
        remaining = self._check_quota(token)
        if remaining > 0:
            return token
```

## 🟡 ВАЖНО (улучшит качество)

### 4. A/B тестирование превью в реальном времени
**Идея:** Загружать 3 превью как unlisted видео, через 24ч смотреть CTR
**Реализация:** YouTube Data API для загрузки + Analytics API для CTR

### 5. Умная музыка по сегментам
**Проблема:** Одна музыка на всё видео
**Решение:** Разная музыка для разных частей (драматичная для кульминации, спокойная для вступления)

### 6. Автоматическое определение длительности сцены
**Проблема:** Фиксированная длительность на картинку
**Решение:** Анализировать текст озвучки, синхронизировать картинки с контентом

## 🟢 ПРИЯТНО ИМЕТЬ

### 7. Шаблоны каналов
**Идея:** Готовые пресеты для разных ниш (военная история, true crime, наука)
**Включает:** Стиль, голос, музыка, эффекты, промпты

### 8. Статистика генерации
**Идея:** Дашборд с метриками
- Сколько видео создано
- Среднее время генерации
- Успешность генерации изображений
- Использование API лимитов

### 9. Экспорт для разных платформ
**Идея:** Автоматическая адаптация для YouTube Shorts, TikTok, Instagram Reels
- Вертикальный формат
- Короткие версии (60 сек)
- Разные соотношения сторон

---

## 📊 Приоритеты

| # | Функция | Сложность | Влияние | Время |
|---|---------|-----------|---------|-------|
| 1 | Whisper субтитры | Средняя | Высокое | 2-3ч |
| 2 | Кэширование | Низкая | Высокое | 1ч |
| 3 | Авто-выбор токена | Низкая | Среднее | 30мин |
| 4 | A/B превью | Высокая | Высокое | 4-5ч |
| 5 | Музыка по сегментам | Средняя | Среднее | 2ч |
