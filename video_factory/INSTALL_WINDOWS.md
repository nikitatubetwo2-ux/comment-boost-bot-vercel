# 🖥️ Установка Video Factory на Windows

## Быстрая установка (5 минут)

### 1. Установи Python 3.10+

Скачай с https://www.python.org/downloads/

**ВАЖНО:** При установке поставь галочку ✅ "Add Python to PATH"

Проверь в командной строке:
```cmd
python --version
```

### 2. Установи FFmpeg

**Вариант A (рекомендуется):** Через winget
```cmd
winget install ffmpeg
```

**Вариант B:** Вручную
1. Скачай с https://ffmpeg.org/download.html
2. Распакуй в `C:\ffmpeg`
3. Добавь `C:\ffmpeg\bin` в PATH

Проверь:
```cmd
ffmpeg -version
```

### 3. Скопируй папку video_factory

Скопируй всю папку `video_factory` на Windows ПК.

Можно через:
- USB флешку
- Google Drive / Dropbox
- Git (если используешь)

### 4. Установи зависимости

Открой командную строку в папке video_factory:
```cmd
cd путь\к\video_factory
pip install -r requirements.txt
```

### 5. Запусти приложение

```cmd
python main.py
```

Или используй `run_windows.bat` (двойной клик)

---

## 📁 Файлы которые нужно скопировать

```
video_factory/
├── .env                 ← ВСЕ API КЛЮЧИ (ВАЖНО!)
├── main.py
├── config.py
├── requirements.txt
├── core/                ← вся папка
├── ui/                  ← вся папка
├── data/                ← профили каналов (если есть)
└── output/              ← можно не копировать (создастся автоматически)
```

**ВАЖНО:** Файл `.env` содержит все API ключи. Он одинаковый на всех ПК!

---

## 🔑 API ключи

Файл `.env` уже содержит все ключи:
- 141 ключ ElevenLabs (~1.4 млн символов/месяц)
- 3 ключа YouTube API
- 16 токенов HuggingFace
- Groq API ключ
- Telegram бот

**Ключи общие для всех ПК** — лимиты делятся между компьютерами!

---

## ⚠️ Важные отличия Windows

### Пути к файлам
Windows использует `\` вместо `/`, но Python обрабатывает это автоматически.

### FFmpeg
Убедись что ffmpeg доступен в PATH:
```cmd
where ffmpeg
```

### Шрифты для субтитров
Если субтитры не работают, установи шрифт Arial или измени в коде на другой.

---

## 🚀 Параллельная работа на нескольких ПК

### Стратегия 1: Разделение задач
- **ПК 1 (Mac):** Проекты 1-5
- **ПК 2 (Windows):** Проекты 6-10

### Стратегия 2: Разделение ключей
Раздели ключи ElevenLabs между ПК:
- **ПК 1:** Первые 70 ключей
- **ПК 2:** Остальные 71 ключ

Для этого отредактируй `.env` на каждом ПК.

### Стратегия 3: Общая очередь (продвинутый)
Используй облачное хранилище для синхронизации:
- Google Drive
- Dropbox
- OneDrive

Синхронизируй папку `output/` для доступа к готовым видео с любого ПК.

---

## 🔧 Решение проблем

### "python не найден"
Переустанови Python с галочкой "Add to PATH"

### "pip не найден"
```cmd
python -m pip install -r requirements.txt
```

### "ffmpeg не найден"
Добавь путь к ffmpeg в системную переменную PATH

### "ModuleNotFoundError"
```cmd
pip install --upgrade -r requirements.txt
```

### Ошибки с PyQt6
```cmd
pip uninstall PyQt6 PyQt6-Qt6 PyQt6-sip
pip install PyQt6
```

---

## 📊 Мониторинг квот

В настройках приложения (вкладка ⚙️) есть:
- Счётчик символов ElevenLabs
- Квота YouTube API
- Кнопка "Обновить квоты"

**Совет:** Проверяй квоты перед запуском большой очереди!

---

## 💡 Советы для максимальной производительности

1. **Запускай ночью** — очередь работает в фоне
2. **Используй SSD** — быстрее рендер
3. **Закрой браузер** — больше RAM для генерации
4. **Telegram уведомления** — узнаешь когда готово

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь что Python 3.10+ установлен
2. Проверь что все зависимости установлены
3. Проверь что .env файл на месте
4. Посмотри логи в консоли
